def test_two(live_server, session_browser):
    print("Test Two.")
    print(f"live_server => {live_server.url}")
    session_browser.get(live_server.url)


def test_two_2(live_server, session_browser):
    print("Test Two.")
    print(f"live_server => {live_server.url}")
    session_browser.get(live_server.url)
